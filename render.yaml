services:
  - type: web
    name: pii-redactor
    env: python
    rootDir: pii_redactor
    buildCommand: pip install -r requirements.txt && python -m spacy download en_core_web_sm
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
